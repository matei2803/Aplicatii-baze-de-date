<UserControl x:Class="expences.mvvm.view.RecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:expences.mvvm.view"
             xmlns:user="clr-namespace:expences.User"
             mc:Ignorable="d" 
             d:DesignHeight="474.473" d:DesignWidth="663.634">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="column1" Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border x:Name="border1" Background="#2874A6" Padding="30" CornerRadius="20, 0, 0, 20">
            <StackPanel VerticalAlignment="Center">
                <Image Source="/images/budget.png" Width="130" Height="130" Margin="0,0,0,40"/>
                <TextBlock Text="Record it" TextAlignment="Center" FontWeight="SemiBold"
                        FontSize="28" Foreground="#363636"/>
                <TextBlock TextWrapping="Wrap" FontSize="16" TextAlignment="Center"
                        Foreground="#363636" Margin="0,20,0,20" Text="Don't spend more than you earn"/>
                <Button Background="#F5B7B1" Height="60" Width="60">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" CornerRadius="600" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#F1948A"/>
                                    <Setter TargetName="border" Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>

                    <Image Source="/images/next.png"/>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Padding="10" Background="#F1948A" CornerRadius="0 20 20 0">
            <Grid>
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Description" Style="{StaticResource text}"/>
                    <TextBlock Grid.Row="1" Text="Amount" Style="{StaticResource text}"/>
                    <TextBlock Grid.Row="2" Text="Date" Style="{StaticResource text}"/>
                    <TextBlock Grid.Row="3" Text="Category" Style="{StaticResource text}"/>


                    <user:MyTextBox x:Name="txtDescription" Grid.Column="1" Grid.Row="0" Hint="KFC"/>
                    <user:MyTextBox x:Name="txtAmount" Grid.Column="1" Grid.Row="1" Hint="25.50"/>
                    <user:MyTextBox x:Name="txtCategory" Grid.Column="1" Grid.Row="3" Hint="Food"/>

                    <DatePicker x:Name="dpDate" Grid.Column="1" Grid.Row="2" Background="#FADBD8" Width="150" Margin="0 15 0 15"/>

                    <Grid Grid.Column="1" Grid.Row="7" Margin="0 20 0 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Cancel" Margin="0 0 10 0" 
                            Style="{StaticResource buttonMain}"
                            Click="Button_Click"/>
                        <Button Content="Save" Margin="10 0 0 0" Grid.Column="1" 
                            Style="{StaticResource buttonMainGreen}"
                            Click="Button_Click"/>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
